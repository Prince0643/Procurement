# PR Bypass Feature Tasks

- [x] Backend: Add `is_bypassed` and `bypassed_by` columns to the database
- [x] Backend: Implement `PUT /api/purchase-requests/:id/bypass` route
- [x] Frontend: Add `bypass` to `purchaseRequestService`
- [x] Frontend: Update `PRPreviewModal.jsx` to render "Bypass Approvals" conditionally
- [x] Frontend: Update UI to show "Bypassed By" information
- [x] Documentation: Update `system-flowchart.md` with bypass flow
- [x] Verification: Verify functionality end-to-endt
- [ ] Frontend: Show "Bypassed By" information in PR details if `is_bypassed` is true
- [ ] Documentation: Update `system-flowchart.md` with bypass flow
